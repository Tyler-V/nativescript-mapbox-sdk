{
  "files.exclude": {
    "**/*.js": { "when": "$(basename).ts" },
    "**/*.map": { "when": "$(basename).map" },
    "**/node_modules": true
  },
  "search.exclude": {
    "**/node_modules": true
  }
}
